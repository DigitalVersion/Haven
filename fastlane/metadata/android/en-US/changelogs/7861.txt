Two tunnel fixes: VNC through an SSH tunnel now connects to the VNC host you configured instead of always dialling the jump host's loopback; and mosh over a Tailscale or WireGuard tunnel now works with hostnames and MagicDNS names — the UDP stream uses the address the tunnel actually connected to, and a truly unresolvable destination fails with a clear message instead of retrying forever.
