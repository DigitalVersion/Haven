🪟 Linux desktops and single-app windows work again. They failed outright on every install — over the agent bridge the failure took the connection down with it. The compositor library Haven ships had been built before part of its link step ran, leaving 51 references unresolved, so Android refused to load it. Nothing caught this at build time; it only failed on a real device. The library is rebuilt, and the build now refuses to package one in that state. (#469)
